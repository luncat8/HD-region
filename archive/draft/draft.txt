draft is not strict rule. may be improved.

----------1
situation:
есть картинки к книге. Можно сделать их фоном для VN. Для этого делается outpaint до большего разрешения (размеры произвольные, где то нужно записать положение и размер оригинальной части).
original image - important ROI. outpainted - just a filler.
Нужно чтобы оригинальная часть картинки гарантированно целиком была на экране при любой ориентации и разрешении. Но Для соотношений не совпадающих с картинкой показывались дорисованные части.
final book is image + HTML5 js - without servers, without node, file:// friendly.
<img id="bg" src="1.png" class="background">
<button onclick="nextImg(originals)">

Я предлагаю такой формат для описания центральной области:
originals['1.png'] = { x: 512, y: 256, w: 768, h: 1024 }; 
то есть w h задают регион который меньше чем img.naturalWidth, img.naturalHeight.

Рисунок всегда должен масштабироваться так чтобы оригинальная область занимала максимум но не вылазила за края и keep aspect ratio. Поведение аналогичное max-width=100vh; max-height=auto для original rect.


должно работать в vanilla js, single html file, fallback if no js. общий вес важен - поэтому тайлы если делать то без повторения, а с black regions

single tab indent for html js
----------2
py scipt для генерации этого тестового файла, закрашивай зоны разными цветами, чтобы видеть правильно ли работает. В начале py файла настройки x: 512, y: 256, w: 768, h: 1024. Помещает в буфер строку типа originals['1.png'] = {...}; для того чтобы я мог вставить в html js код.

----------3
py scipt
обрабатывает все jpg png webp файлы в текущей папке и подпапках (кроме анимированых webp).
Ищет	filename_c.extension  Например "1_c.png"
вычисляет	filename_c	1_c
вычисляет	filename	1
ищет	filename.extension	1.png
если он есть - CV определяет его позицию (1.png) в рисунке filename_c.extension "1_c.png"
Помещает в буфер js код объектов originals, который можно вставить в html. (не json, чтобы лишний раз не парсить строку)


если экран меньше или больше оригинальной области - масштаб.

typical bugs:

баг в html скрипте. Он делает translate(-372.333px, 0px) scale(0.825833)   . Визуально это не верно. смещено влево. При жесте вроде центрируется то масштаб опять не верный


баг в поиске файлов. не находит 
надо
для каждого файла _c перед расширением - то найти рисунок без _c 


 